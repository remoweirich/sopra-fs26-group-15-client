export interface Train {
  currentX: number;
    currentY: number;
    trainId: string;
    timeStamp: number;
    line: string;
    lineOrigin: string;
    lineDestination: string;
    departureTime: number;
    arrivalTime: number;

  fromStation: string;
  toStation: string;
  
}